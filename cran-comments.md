## Test environments
* local R installation, R 4.3.3
* ubuntu-latest (via GitHub Actions), R-devel, R-release, R-oldrel-1
* macOS-latest (via GitHub Actions), R-release
* windows-latest (via GitHub Actions), R-release

## R CMD check results

0 errors | 0 warnings | 0 notes

* This is a new release.

## Downstream dependencies

There are currently no downstream dependencies for this package.

## Notes

* This package is an R port of the Python isimip-client library
* All functionality from the Python version has been implemented using R's httr2 package
* The package includes comprehensive tests and documentation
* A pkgdown website is configured and will be deployed via GitHub Actions
